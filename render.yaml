services:
  - type: web
    name: antarctic-frontend
    runtime: static
    buildCommand: "true"
    staticPublishPath: .
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
